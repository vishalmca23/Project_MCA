<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DOC-MED-CO | Orders List</title>
  <link href="../header.css" rel="stylesheet" type="text/css" />
  <link href="../sidenav.css" rel="stylesheet" type="text/css" />
  <link href="../tables.css" rel="stylesheet" type="text/css" />
</head>

<header>
  <div class='main'>
    <%- include('../../partials/patient/header') %>
  </div>
</header>

<body>
  <%- include('../../partials/patient/sidenav') -%>

  <div class='main'>
    <h1><center><i>Doctor Medical Collaboration</i></center></h1>
    <br>
    <%= message %>
    <% if(!message && details.length) { %>
    <h2>My Orders</h2>
    <br>
    <table>
      <th>Medicine</th>
      <th>Quantity</th>
      <th>Date</th>
      <th>Amount</th>
      <th>Supplier</th>
      <th>Status</th>
      <% details.forEach(order => { %>
      <tr>
        <td><%= order.medicineName %></td>
        <td><%= order.quantity %></td>
        <td><%= order.date %></td>
        <td><%= order.amount %></td>
        <td>
          <% if (order.supplierId) { %>
          <a href="/patient/supplier-information/<%= order.supplierId%>">View details</a>
          <% } %>
        </td>
        <td><%= order.status %></td>
      </tr>
      <% }) %>
      <% } %>
  </div>
</body>

</html>
