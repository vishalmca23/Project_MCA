<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DOC-MED-CO | Orders</title>
  <link href="../header.css" rel="stylesheet" type="text/css" />
  <link href="../sidenav.css" rel="stylesheet" type="text/css" />
  <link href="../tables.css" rel="stylesheet" type="text/css" />
</head>

<header>
  <div class='main'>
    <%- include('../../partials/supplier/header') %>
  </div>
</header>

<body>
  <%- include('../../partials/supplier/sidenav') -%>

  <div class='main'>
    <h1><center><i>Doctor Medical Collaboration</i></center></h1>
    <br>
    <h2>Orders</h2>
    <br>
    <%= message %>
    <% if(!message && details.length) { %>
      <table>
        <th>Patient</th>
        <th>Medicine</th>
        <th>Quantity</th>
        <th>Date</th>
        <th>Amount</th>
        <th>Status</th>
        <th>Delivered</th>
        <% details.forEach(order => { %>
          <form action="/supplier/order-delivered/<%= order.id %>">
          <tr>
            <td><a href="/supplier/patient-information/<%= order.patientId%>">View Information</a></td>
            <td><%= order.medicineName %></td>
            <td><%= order.quantity %></td>
            <td><%= order.date %></td>
            <td><%= order.amount %></td>
            <td><%= order.status %></td>
            <td><button type="submit">Click</button></td>
          </tr>
        </form>
        <% }) %>
        <% } %>
  </div>
</body>

</html>
